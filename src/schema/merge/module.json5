/**
 * Modifies the qualifying the $id. Merged with `abstract/base-packs.json5` & `abstract/module.json5`
 * modifying the packs->items->entity type to be a string.
 */
{
  "$id": "shared/definitions/module.json",

  "definitions": {
    "packs": {
      "items": {
        "properties": {
          "entity": {
            "type": "string"
          },
          "system": {
            "errorMessage": "should be a string or array of strings"
          }
        }
      }
    },
    "system": {
      "errorMessage": "should be a string or array of strings"
    }
  }
}
